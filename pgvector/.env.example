# Standalone PostgreSQL 17 + pgvector extension.
# Separate from the default "postgres" (16) service — nothing else in this
# repo connects to this instance. Uses port 5433 to avoid clashing with
# Postgres 16 on 5432.
RESTART_POLICY=always
POSTGRES_USER=postgres
POSTGRES_PASSWORD=Password102!
POSTGRES_DB=postgres
POSTGRES_PORT=5433
POSTGRES_CPUS_LIMIT=0.5
POSTGRES_MEMORY_LIMIT=512M
POSTGRES_CPUS_RESERVED=0.1
POSTGRES_MEMORY_RESERVED=128M
