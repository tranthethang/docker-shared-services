services:
  postgres:
    container_name: postgres
    image: postgres:16-alpine
    restart: "${RESTART_POLICY:-always}"
    deploy:
      resources:
        limits:
          cpus: "${POSTGRES18_CPUS_LIMIT:-1}"
          memory: "${POSTGRES18_MEMORY_LIMIT:-1G}"
        reservations:
          cpus: "${POSTGRES18_CPUS_RESERVED:-0.5}"
          memory: "${POSTGRES18_MEMORY_RESERVED:-512M}"
    environment:
      POSTGRES_USER: "${POSTGRES18_USER:-postgres}"
      POSTGRES_PASSWORD: "${POSTGRES18_PASSWORD:-password102}"
      POSTGRES_DB: "${POSTGRES18_DB:-postgres_db}"
      TZ: "${TZ:-Asia/Ho_Chi_Minh}"
    ports:
      - "${POSTGRES18_PORT:-5433}:5432"
    networks:
      - dev_tools
    volumes:
      - ./postgres/bin:/usr/local/bin/postgres-scripts:ro
      - postgres_data:/var/lib/postgresql/data
    command:
      - sh
      - -c
      - |
        (
          sleep 10
          until pg_isready -U "$${POSTGRES_USER:-postgres}" -q; do
            sleep 2
          done
          /usr/local/bin/postgres-scripts/init.sh
        ) &
        exec docker-entrypoint.sh postgres
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES18_USER:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5

networks:
  dev_tools:
    name: dev_tools
    external: true

volumes:
  postgres_data:
